input {

	udp {
		port => 5000
	}
}

filter {
	json {
		source => "message"
	}
	mutate {
		convert => {
			"[data][workflow_args_hash]" => "string"
			"[data][task_hash]" => "string"
			"[data][arg_mapping_hash]" => "string"
			"[data][dag_id]" => "integer"
			"[data][task_id]" => "integer"
			"[data][task_instance_id]" => "integer"
			"[data][task_template_id]" => "integer"
			"[data][tool_id]" => "integer"
			"[data][tool_version_id]" => "integer"
			"[data][workflow_id]" => "integer"
			"[data][workflow_run_id]" => "integer"
	    }
	}
	date {
		match => [ "timestamp", "yyyy-MM-dd HH:mm:ss"]
	}
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => ["${ELASTICSEARCH_HOSTS}"]
		index => "jobmon"
	}
}
